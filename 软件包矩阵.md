# 软件包矩阵构建表

配置来源：`conf/repo.conf`  
发布地址：https://luhaikong2024.github.io/apt-repo/  
组件：`main`

---

## 1. 目标矩阵（计划支持）

维度：**套件（Ubuntu 版本）× 架构（CPU）**

| 套件 | Ubuntu | amd64（x86_64） | arm64（ARM64） |
|------|--------|-----------------|----------------|
| `bionic` | 18.04 | 计划支持 | 计划支持 |
| `focal` | 20.04 | 计划支持 | 计划支持 |
| `jammy` | 22.04 | 计划支持 | 计划支持 |
| `noble` | 24.04 | 计划支持 | 计划支持 |

共 **4 × 2 = 8** 个构建/发布格子。  
客户端 `apt` 会按本机 `dpkg --print-architecture` 自动选对应格子，无需手写路径。

索引路径规则：

```text
dists/<套件>/main/binary-<架构>/Packages.gz
pool/main/<首字母>/<包名>/<包名>_<版本>_<架构>.deb
```

---

## 2. 当前已发布状态（线上）

图例：`✅` 已有包 · `⬜` 空（索引在，但无软件）

### 总览

| 套件 \\ 架构 | amd64 | arm64 |
|--------------|-------|-------|
| bionic       | ⬜    | ⬜    |
| focal        | ⬜    | ⬜    |
| jammy        | ✅    | ⬜    |
| noble        | ⬜    | ⬜    |

### 已上架软件明细

| 套件 | 架构 | 包名 | 版本 | 来源 | pool 路径 |
|------|------|------|------|------|-----------|
| jammy | amd64 | hello | 2.10-2ubuntu4 | `incoming/jammy/` | `pool/main/h/hello/hello_2.10-2ubuntu4_amd64.deb` |

说明：目前仓库里**只有这一个软件**；其余格子仅有空索引骨架（含 bionic）。

---

## 3. 两种装包进矩阵的方式

### 方式 A：预置 deb（`incoming/`，适合现成包 / 试跑）

```text
incoming/
├── bionic/*.deb     # Ubuntu 18.04
├── focal/*.deb      # Ubuntu 20.04
├── jammy/*.deb      # Ubuntu 22.04 ← 当前有 hello_…_amd64.deb
└── noble/*.deb      # Ubuntu 24.04
```

推送到 GitHub 后，Actions 会按目录名导入对应套件，再生成 `dists/` 并发布 Pages。

| 要填充的格子 | 操作 |
|--------------|------|
| bionic + amd64 | 把 `*_amd64.deb` 放进 `incoming/bionic/` |
| jammy + amd64 | 把 `*_amd64.deb` 放进 `incoming/jammy/` |
| jammy + arm64 | 把 `*_arm64.deb` 放进 `incoming/jammy/` |
| … | 以此类推 |

同一套件下可放多个不同软件的 `.deb`；架构靠文件名/包内 `Architecture` 字段区分。

### 方式 B：从源码构建（`source/` + Docker，适合自有软件）

```bash
make package S=<套件> A=<架构>
# 例：
make package S=bionic A=amd64
make package S=jammy A=amd64
make package S=jammy A=arm64
make package S=focal A=amd64
make package S=noble A=amd64
```

全量（配置里全部套件 × 架构）：

```bash
make all
# 或带工具链：
./scripts/build-all.sh --with-toolchain
```

版本约定：上游版本 `PACKAGE_VERSION`（如 `1.0.0`）在各套件会变成：

| 套件 | 打进 deb 的 Version |
|------|---------------------|
| bionic | `1.0.0~bionic1` |
| focal | `1.0.0~focal1` |
| jammy | `1.0.0~jammy1` |
| noble | `1.0.0~noble1` |

（避免不同系统编译产物在 `pool/` 里文件名冲突。）

---

## 4. 自有软件 `myapp` 的目标矩阵（待源码）

配置：`PACKAGE_NAME=myapp`，`PACKAGE_VERSION=1.0.0`

| 套件 \\ 架构 | amd64 | arm64 |
|--------------|-------|-------|
| bionic | ⬜ 待构建 `myapp_1.0.0~bionic1_amd64.deb` | ⬜ 待构建 `…_arm64.deb` |
| focal | ⬜ 待构建 `myapp_1.0.0~focal1_amd64.deb` | ⬜ 待构建 `…_arm64.deb` |
| jammy | ⬜ 待构建 `myapp_1.0.0~jammy1_amd64.deb` | ⬜ 待构建 `…_arm64.deb` |
| noble | ⬜ 待构建 `myapp_1.0.0~noble1_amd64.deb` | ⬜ 待构建 `…_arm64.deb` |

前置条件：

1. `source/` 放入可编译源码  
2. `packaging/myapp/debian/` 打包元数据已就绪（debhelper >= 11，兼容 18.04）  
3. （可选）`toolchain_source/` 编好后按套件/架构挂载  

---

## 5. CI 构建矩阵

工作流默认：导入 `incoming/` → 生成仓库 → 部署 Pages。  

完整构建意图（套件 × 架构）：

| Suite | Arch |
|-------|------|
| bionic | amd64 |
| bionic | arm64 |
| focal | amd64 |
| focal | arm64 |
| jammy | amd64 |
| jammy | arm64 |
| noble | amd64 |
| noble | arm64 |

共 8 路。Docker 镜像：`docker/Dockerfile.bionic` 等。

---

## 6. 如何核对矩阵是否填满

```bash
# 列出 jammy/amd64 下所有软件名
curl -fsSL https://luhaikong2024.github.io/apt-repo/dists/jammy/main/binary-amd64/Packages \
  | awk '/^Package:/{print $2}'
```

扫全部 8 格：

```bash
for s in bionic focal jammy noble; do
  for a in amd64 arm64; do
    echo -n "$s/$a: "
    curl -fsSL "https://luhaikong2024.github.io/apt-repo/dists/$s/main/binary-$a/Packages" \
      | awk '/^Package:/{print $2}' | paste -sd, -
    echo
  done
done
```

---

## 7. 维护备忘

| 项 | 位置 |
|----|------|
| 改支持的套件/架构 | `conf/repo.conf` → `SUITES` / `ARCHITECTURES` |
| 预置包 | `incoming/<套件>/*.deb` |
| 自研包名/版本 | `conf/repo.conf` → `PACKAGE_NAME` / `PACKAGE_VERSION` |
| 打包规则 | `packaging/<包名>/debian/` |
| 18.04 构建镜像 | `docker/Dockerfile.bionic` |
| 签名私钥备份 | 私有仓 `apt-repo-key` + GitHub Secret `APT_REPO_GPG_PRIVATE_KEY` |

更新本表：每次正式发布新包后，改 **§2 当前已发布状态** 即可。
