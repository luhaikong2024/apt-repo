# apt-repo 使用说明（分发仓）

本仓库只做一件事：把 `.deb` 做成官方布局的 APT 源，并挂到 GitHub Pages。

编包、工具链、源码 → 私有仓 **apt-repo-toolchain**。

---

## 1. 和构建仓的关系

```text
apt-repo-toolchain（私有）          apt-repo（本仓 / Pages）
  source/                            incoming/<套件>/*.deb
  toolchain_source/        make sync        │
  toolchains/             ──────────►       ▼
  packaging/ + docker                   import → dists/ + pool/
  outgoing/<套件>/*.deb                 签名 → public/ → Pages
```

---

## 2. 本仓目录

| 路径 | 作用 |
|------|------|
| `incoming/` | 待发布 `.deb` |
| `conf/repo.conf` | 套件 / 架构 / GPG |
| `scripts/` | 导入、索引、签名、发布 |
| `keys/` | 公钥；私钥在 `keys/gnupg/` |
| `repo/` · `public/` | 生成结果 |

---

## 3. 日常发布

```bash
# A. 构建仓
cd ../apt-repo-toolchain
make package S=jammy A=amd64
make sync-publish          # 写入本仓 incoming 并生成本地 public/

# B. 本仓推送（触发 Pages）
cd ../linux_apt_repo   # 或你的 apt-repo 路径
git add incoming
git commit -m "Add packages"
git push
```

仅更新索引：`make all`。

---

## 4. 客户端（正规：先公钥）

```bash
curl -fsSL https://luhaikong2024.github.io/apt-repo/add-apt-source.sh \
  | sudo bash -s -- https://luhaikong2024.github.io/apt-repo
sudo apt install <包名>
```

apt 按本机架构选 `binary-amd64` / `binary-arm64`；套件名来自 sources 里的 `jammy`/`bionic` 等（脚本用 `lsb_release -cs`）。

---

## 5. 官方布局（简要）

```text
public/
├── dists/<套件>/InRelease
├── dists/<套件>/main/binary-<架构>/Packages.gz
├── pool/main/.../*.deb
└── repo-key.gpg
```

GPG：私钥只放构建机 / GitHub Secret / 私有备份仓 `apt-repo-key`；用户只装公钥验章。

---

## 6. 密钥备份

- 私钥备份：https://github.com/luhaikong2024/apt-repo-key（私有）  
- Pages Secret 名：`APT_REPO_GPG_PRIVATE_KEY`
